set(TEST_ENVIRONMENT_VARS "QT_QPA_PLATFORM=offscreen")
add_subdirectory(core)
if(${SETTINGSBUS_BUILD_APP})
  add_subdirectory(app)
endif()
if(${SETTINGSBUS_BUILD_DAEMON})
  add_subdirectory(daemon)
endif()